---
# apps/starship/tasks/install.yaml

# ── macOS (Homebrew) ──────────────────────────────────────────────────────────
- name: "Install starship via Homebrew (macOS)"
  community.general.homebrew:
    name: starship
    state: present
  when: ansible_facts['os_family'] == 'Darwin'
