# tasks.yaml

# put all of the non-dotfile configuration and setup work to be done for this app here.
# This file will be included in the main playbook.yaml file.

- name: Ensure fnt dependencies are installed via Homebrew
  community.general.homebrew:
    name: xz
    state: present

- name: Ensure fnt is installed via Homebrew
  community.general.homebrew:
    name: fnt
    state: present
