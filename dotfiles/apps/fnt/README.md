# fnt: a apt package manager for fonts

It works on brew and linux!!

# brew requirements
install `xz` separately so it can work with debian fonts

source: https://github.com/alexmyczko/fnt/issues/26

# installation
package and req's should be put into the `./install.zsh` script