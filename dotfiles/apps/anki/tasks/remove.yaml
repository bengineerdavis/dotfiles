  🤖 rewrite to template: apps/anki/tasks/remove.yaml
  🤖 trying: devstral-small-2:24b
  🤖 ✅ success: devstral-small-2:24b
---
# apps/anki/tasks/remove.yaml
# Explicit uninstall tasks. Never run as part of provision or install.
# Only invoked via --tags remove.
# Mirror the install.yaml structure — one block per package manager.

# ── macOS (Homebrew) ───────────────────────────────────────────────────────────
# - name: "Remove anki via Homebrew (macOS)"
#   community.general.homebrew:
#     name: anki
#     state: absent
#   when: ansible_facts['os_family'] == 'Darwin'

# ── macOS (Homebrew Cask) ──────────────────────────────────────────────────────
- name: "Remove anki via Homebrew Cask (macOS)"
  community.general.homebrew_cask:
    name: anki
    state: absent
  when: ansible_facts['os_family'] == 'Darwin'

# ── Linux (apt) ────────────────────────────────────────────────────────────────
# - name: "Remove anki via apt (Linux)"
#   ansible.builtin.apt:
#     name: anki
#     state: absent
#   become: true
#   when: ansible_facts['os_family'] == 'Debian'

# ── Docker ─────────────────────────────────────────────────────────────────────
# - name: "Remove anki Docker image"
#   community.docker.docker_image:
#     name: anki:latest
#     state: absent
#   when: install_method | default('') == 'docker'
