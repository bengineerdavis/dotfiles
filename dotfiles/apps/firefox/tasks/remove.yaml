# apps/firefox/tasks/remove.yaml
# Explicit uninstall tasks. Never run as part of provision or install.
# Only invoked via --tags remove.
# Mirror the install.yaml structure — one block per package manager.

# ── macOS (Homebrew Cask) ───────────────────────────────────────────────────
- name: "Remove firefox via Homebrew Cask (macOS)"
  community.general.homebrew_cask:
    name: firefox
    state: absent
  when: ansible_facts['os_family'] == 'Darwin'

# ── macOS (Homebrew) ───────────────────────────────────────────────────────
# - name: "Remove firefox via Homebrew (macOS)"
#   community.general.homebrew:
#     name: firefox
#     state: absent
#   when: ansible_facts['os_family'] == 'Darwin'

# ── Linux (apt) ─────────────────────────────────────────────────────────────
# - name: "Remove firefox via apt (Linux)"
#   ansible.builtin.apt:
#     name: firefox
#     state: absent
#   become: true
#   when: ansible_facts['os_family'] == 'Debian'

# ── Docker ─────────────────────────────────────────────────────────────────
# - name: "Remove firefox Docker image"
#   community.docker.docker_image:
#     name: firefox:latest
#     state: absent
#   when: install_method | default('') == 'docker'
