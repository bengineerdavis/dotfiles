PROVENANCE: reworded on 2026-08-21.

This commit previously bore the pii-redactor three-layer subject while
containing only these 52 zsh deletions — two sessions sharing one
.git/index. The pii-redactor message now sits on 0bf73dc with the code it
describes; this one describes what it actually deletes.

Contents unchanged: the same 52 files, verified byte-identical to the
pre-rewrite tree. Pre-rewrite history is on backup/pre-unscramble-272bdf7.

Prevention: `git commit -o <paths>`, then `git show --stat HEAD`. See
AGENTS.md, "Working alongside other sessions".
